# requirements.txt
# This file mirrors the pinned runtime dependencies in pyproject.toml.
# Python version is enforced via pyproject.toml: ==3.10.17

numpy==2.2.6
pandas==2.2.3
matplotlib==3.10.3
pyzx==0.9.0
